global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'cinema-plus-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['backend:8080']
    # - targets: ['host.docker.internal:8080']
